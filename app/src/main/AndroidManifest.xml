<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher_phone_gateway"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".ProxyForegroundService"
            android:enabled="true"
            android:exported="true"
            android:foregroundServiceType="specialUse"
            tools:ignore="ExportedService">
            <intent-filter>
                <action android:name="com.wsy.pixelproxygateway.action.START" />
                <action android:name="com.wsy.pixelproxygateway.action.STOP" />
                <action android:name="com.wsy.pixelproxygateway.action.RESTART" />
                <action android:name="com.wsy.pixelproxygateway.action.APPLY_CONFIG" />
                <action android:name="com.wsy.pixelproxygateway.action.CHECK_HEALTH" />
            </intent-filter>
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="User-controlled LAN HTTP/SOCKS proxy gateway supervised by in-app watchdogs." />
        </service>

        <receiver
            android:name=".ControlReceiver"
            android:enabled="true"
            android:exported="true"
            tools:ignore="ExportedReceiver">
            <intent-filter>
                <action android:name="com.wsy.pixelproxygateway.action.START" />
                <action android:name="com.wsy.pixelproxygateway.action.STOP" />
                <action android:name="com.wsy.pixelproxygateway.action.RESTART" />
                <action android:name="com.wsy.pixelproxygateway.action.APPLY_CONFIG" />
                <action android:name="com.wsy.pixelproxygateway.action.CHECK_HEALTH" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <provider
            android:name=".StatusProvider"
            android:authorities="com.wsy.pixelproxygateway.status"
            android:enabled="true"
            android:exported="true"
            tools:ignore="ExportedContentProvider" />
    </application>
</manifest>
